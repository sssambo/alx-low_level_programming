#include <string.h>
/**
 * _strlen - counts the total length of passed
 *		argument
 *
 * @s: char pointer
 *
 * Return: len
 */

int _strlen(char *s)
{
	int len;

	len = strlen(*s);

	return (len);

}
